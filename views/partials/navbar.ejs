<nav class="navbar navbar-expand-lg">
  <div class="container">
    <!-- Brand -->
    <a class="navbar-brand" href="/">Movies</a>

    <!-- Mobile toggler -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Links -->
    <div class="collapse navbar-collapse" id="navbarContent">
      <!-- Left side -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" href="/movies">All Movies</a>
        </li>

        <% if (currentUser) { %>
        <li class="nav-item">
          <a class="nav-link" href="/movies/new">Add Movie</a>
        </li>
        <% } %>
      </ul>

      <!-- Right side (auth) -->
      <ul class="navbar-nav ms-auto align-items-center">
        <% if (!currentUser) { %>
        <li class="nav-item me-2">
          <a class="btn-nav outline" href="/register">Register</a>
        </li>

        <li class="nav-item">
          <a class="btn-nav" href="/login">Login</a>
        </li>

        <% } else { %>

        <li class="nav-item">
          <span class="navbar-text me-3 user-greeting">
            Hello, <%= currentUser.name %>
          </span>
        </li>

        <li class="nav-item">
          <form action="/logout" method="post" class="m-0">
            <button class="btn-nav outline" type="submit">Logout</button>
          </form>
        </li>

        <% } %>
      </ul>
    </div>
  </div>
</nav>
